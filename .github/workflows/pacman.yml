name: Generate Pacman Graph

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: vitorjohnny
          outputs: |
            dist/pacman-contribution-graph.svg?animation=pacman&color_snake=#f2c94c
            dist/pacman-contribution-graph-dark.svg?palette=github-dark&animation=pacman

      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist
          publish_branch: output
